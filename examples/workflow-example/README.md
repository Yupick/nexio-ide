# Workflow example

This example documents the intended agent flow:

1. User describes a feature in the Ideas agent.
2. The Planning agent converts the feature into a structured roadmap.
3. The Principal agent delegates tasks to available plugins.
4. Plugins produce diffs and the user approves or rejects them.

This keeps the system traceable and safe by default.
