# Example plugin

This folder is a template for future plugin implementations.

## Purpose

- Add a new agent plugin under `src/plugins`.
- Export a default factory that returns an instance with `init` and `execute`.
- Keep plugin behavior restricted to the agreed sandbox and task contract.
